// *** ARRAYS *** //

// How can we store a list?
const first = 'buy groceries';
const second = 'take out trash';
const third = 'wash laundry';

//what is an array
// contains a list of items
//ordered, and accessible by numbers

  //array
let toDoList = [first, second, third]


console.log(toDoList)


console.log(toDoList[2])
console.log(toDoList.length)
//to find the type of
console.log(typeof toDoList.length)

//to find last item of array toDoList.length - 1

// What is an array? What syntax does it use?

// What can we keep in an array?
     //any data type

// How can we access the items in an array?
let nestedArr = [1, [2, [3]]]

console.log(nestedArr[1][1][0])

// How can we access the end of an array?

// METHODS //

let methodsEx = [1, 2, 3]

// push
//adds the item to the end of the array
methodsEx.push(12)
console.log(methodsEx)

// pop
    //removes the last item off of the array

// unshift
    //allows you to add values to the beginning of the array

// shift
    //removes the first item of an array

// slice


// splice
